#include <stdio.h>
#include "algo.h"

#define WLEN 30 //word length
#define TLEN 256 //text length

int main() {
  // declaring word
  char word[WLEN];
  char text[TLEN];

  // reading strings
  scanf("%s", word);
  scanf("%s", text);

  return 0;
}
